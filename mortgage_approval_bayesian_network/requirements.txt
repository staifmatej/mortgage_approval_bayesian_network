# Python 3.12.2

# Core dependencies with exact versions.
pgmpy==1.0.0
pandas==2.2.3
numpy==2.1.3
networkx==3.4.2
scipy==1.15.2

# Diagram photo.
matplotlib==3.10.1
seaborn==0.13.2

# Diagram photo from pgmpy.
# pygraphviz installation: brew install graphviz (on MacOS) / choco install graphviz (on Windows) / sudo apt-get update && sudo apt-get install -y graphviz graphviz-dev (on Linux)
# pygraphviz==1.14

# Progress bar, table & color.
colorama==0.4.6
tabulate==0.9.0
tqdm==4.66.1

# Development dependencies.
pytest==8.3.4
pylint==3.2.5

# Evaluation dependencies.
scikit-learn==1.5.2